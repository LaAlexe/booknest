# API contracts

This workspace is reserved for API contracts shared through an explicit public boundary. Domain persistence models must not be exported to the frontend.

No contracts are defined in Phase 1.
